apiVersion: apps/v1
kind: Deployment
metadata:
  name: spring-login-deployment
spec:
  selector:
    matchLabels:
      app: login-deployment
  replicas: 2
  template:
    metadata:
      labels:
        app: login-deployment
    spec:
      containers:
        - name: login-deployment
          image: wechatcontainerregistry.azurecr.io/login-image:1.0
          ports:
            - containerPort: 8080
          envFrom:
            - configMapRef:
                name: spring-login-config
          resources:
            requests:
              cpu: 200m

---
apiVersion: autoscaling/v1
kind: HorizontalPodAutoscaler
metadata:
  name: spring-login-autoscaling
  namespace: default
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: spring-login-deployment
  minReplicas: 1
  maxReplicas: 10
  targetCPUUtilizationPercentage: 50
